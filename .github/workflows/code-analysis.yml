name: "ZeroNorth Scanning"

on:
  push:
  pull_request:
  schedule:
    - cron: '0 18 * * 6'

jobs:
  ZeroNorth-Scan:
    runs-on: ubuntu-latest
    #container: zeronorth/integration:latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
    
  # - name: Run ZN IC
    - name: NPM Install
      run: |
        npm install
    - name: Run ZN IC
      run: | 
        docker run -e POLICY_ID=Ht27RaViRyWCATVfanRbKg -v $GITHUB_WORKSPACE:/code -v $GITHUB_WORKSPACE:/results -e INCLUDE_FILES=/code/project.zip -e LOG_LEVEL=DEBUG -e CYBRIC_API_KEY=${{ secrets.ZN_API_KEY }} zeronorth/integration:latest python cybric.py        


    
